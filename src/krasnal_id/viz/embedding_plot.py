"""UMAP and t-SNE embedding-space visualization placeholder."""

from pathlib import Path

from krasnal_id.config import AppConfig


def create_embedding_plot(config: AppConfig) -> Path:
    """Create and save the configured two-dimensional embedding projection."""
    raise NotImplementedError("Embedding visualization is scheduled for v0.2")
